1. Clear environment

2. Manually input:
  -Title (change AgWQ management area name) [line 2]
  -Date [line 4]
  -input$select on line 40, this is the name of the AgWQ Management Area
  -input$dates on line 41, this is the date range to query
  
  
3. push 'Knit' and an html document will be created

4. Add beneficial uses for pH, DO, and temp as needed. Note that the report will list a table of stations that need beneficial uses under each parameter section. 